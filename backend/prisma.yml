endpoint: https://meeter-production-638bfa5d67.herokuapp.com/prod
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
# Deploy hook
hooks:
  post-deploy:
    - graphql get-schema --project database
